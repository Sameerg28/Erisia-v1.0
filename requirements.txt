python-dotenv
openai
groq
tavily-python
chromadb
docker
networkx
psutil
pyautogui
Pillow
requests
schedule
feedparser
pandas
yfinance
pytest==7.4.3
pytest-mock==3.12.0
# v0.2 additions
httpx                    # Faster Ollama health checks (optional)
python-telegram-bot    # Telegram channel (Phase 5)
fastapi                # REST API server (Phase 5)
uvicorn                # ASGI server for FastAPI (Phase 5)
mcp                    # Model Context Protocol server (Phase 6)
mcp-server-fetch       # Web content fetching (Phase 6 bridge)
mcp-server-time        # Time/timezone utilities (Phase 6 bridge)
# Voice/TTS/STT support
edge-tts               # Text-to-Speech using Edge Neural voices
pygame                 # Audio playback for TTS
faster-whisper         # Local speech-to-text (CPU-optimized)
SpeechRecognition      # Speech recognition (for microphone input)
